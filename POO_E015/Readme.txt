Ejercicio 15:

En la consigna se aprecia una imagen:

Un login con un campo para usuario y otro para su contraseña. Este contiene un boton que hace que si se loguea
"admin" y su clave es "1111", se muestra un formulario con campos para legajo, nombre y apellido.
El formulario tiene un "Captcha" que muestra un numero aleatorio entre 1 y 10000. Si se ha completado el captcha,
los campos del formulario y se toca el boton "Alta" del mismo, se cierra la app.

En el login, en caso de loguearse uno diferente al que esta Hard-Codeado, se cierra la app.